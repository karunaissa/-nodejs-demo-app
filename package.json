{
  "name": "ci-node-docker-app",
  "version": "1.0.0",
  "description": "Minimal Express app for CI/CD assignment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.x",
    "supertest": "^6.x"
  },
  "engines": {
    "node": ">=18"
  }
}
